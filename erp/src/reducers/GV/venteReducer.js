import { ADD_CLIENT_SUCCESS } from "../../actions/GV/types";

const initState = {
  client: [],
  errors: null,
};
const venteReducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default venteReducer;
