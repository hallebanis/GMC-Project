const initState = {
  facture: [],
  errors: null,
};

const achatReducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default achatReducer;
